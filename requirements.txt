# Dependencies for gee_export_optimized.py and mosaic_geotiff_tiles.py
# Install after creating a new conda env: pip install -r requirements.txt

earthengine-api>=1.6.0
rasterio>=1.2.0
tqdm>=4.60.0
gdal>=3.8.0
